// CURRENTLY DEPRECATED

/*function UserInput(p)
{
    this.currentInput = 0
    this.holding = false

    this.checkInput = function(keyCode)
    {   
        print(this.holding)

        if (keyCode == 0)
        {
            this.holding = false
            return
        }

        else if ((keyCode == this.currentInput))
        {
            this.holding = true
        }

        else
        {
            this.holding = false
        }

        this.currentInput = keyCode
        print(this.currentInput)
    }
}*/